# Ambient Occlusion Demo

## Notes

This demo showcases the implementation of Ambient Occlusion (AO) in a 3D rendering context. The initial start up of this demo is slow as the AO calculations are performed on the CPU. This is an issue inherited from the original implementation of the demo rather than introduced by the port.